<!DOCTYPE html>
<html>
  <head>
     <% include partials/head %>
  </head>
  <body>
    <div class="container-fluid">
    	<div class="row">
    		<div class="col-12">

          <% include partials/navigation %>
          <h2><%= page %></h2>
            
          <% if (user.host_id == host_id) { %>
          
          <% } else { %>
            <h5>You do not have access to other user's booking information.</h5>
          <% } %>

    	</div>
	</div>
  </body>
</html>
